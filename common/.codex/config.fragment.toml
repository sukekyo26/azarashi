# notify は exec へ直接渡るため ~ や $HOME を展開しない。HOME が可変な dotfiles
# 配布に対応するため bash -c ラッパーで実行時に解決する（JSON ペイロードは "$@"）。
notify = ["bash", "-c", "exec \"$HOME/.codex/notify.sh\" \"$@\"", "codex-notify"]

[features]
hooks = true

[mcp_servers.context-mode]
command = "context-mode"

[mcp_servers.context-mode.env]
CONTEXT_MODE_PLATFORM = "codex"

[tui]
status_line = ["model-with-reasoning", "current-dir", "git-branch", "permissions", "context-used", "five-hour-limit", "weekly-limit", "codex-version"]
status_line_use_colors = true
