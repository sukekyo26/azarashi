approval_policy = "on-request"
approvals_reviewer = "auto_review"

[features]
hooks = true

# Serena は tag 固定で取得する。プラグイン版 (.mcp.json) は git のデフォルトブランチを
# 引くため、レビューを経ない上流変更がそのまま実行される。
[mcp_servers.serena]
command = "uvx"
args = ["--from", "git+https://github.com/oraios/serena@v1.7.0", "serena", "start-mcp-server"]

[mcp_servers.serena.env]
SERENA_USAGE_REPORTING = "false"

[tui]
status_line = ["model-with-reasoning", "current-dir", "git-branch", "permissions", "context-used", "five-hour-limit", "weekly-limit", "codex-version"]
status_line_use_colors = true
# Claude Code 側 Notification と同等の注意喚起。TUI 本体が端末へ BEL を出すため、
# 子プロセス（notify）経由と違い確実に鳴る。フォーカス有無に関わらず鳴らす。
notifications = true
notification_method = "bel"
notification_condition = "always"
