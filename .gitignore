# Editor / OS
*.swp
*~
.DS_Store

# backups created by ./dotfiles (only created under $HOME, but ignore defensively)
*.dotfiles-bak.*

# Python bytecode (the vendored tomlkit regenerates these at runtime)
__pycache__/
*.pyc

*.code-workspace
